import React from 'react'
import ReactDOM from 'react-dom/client'
import TrainingProgram from './TrainingProgram.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TrainingProgram />
  </React.StrictMode>,
)